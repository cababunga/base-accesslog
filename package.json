{
  "name": "@cababunga/accesslog",
  "version": "0.5.1",
  "description": "Accesslog middleware",
  "scripts": {
    "test": "mocha *.test.js",
    "bump": "npm version ${BUMP:=patch} && git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cababunga/base-accesslog"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.25"
  }
}
